from setuptools import setup, find_packages

setup(
    name="coinbase-agentkit-mcp",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "coinbase-agentkit",
        "mcp>=0.5.0",
    ],
)